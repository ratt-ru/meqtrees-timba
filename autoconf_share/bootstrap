#!/bin/sh
if test $# != 1; then
  echo usage: bootstrap autoconf_share_dir
  exit 1
fi
set -x
aclocal -I $1 \
&& libtoolize --force --copy \
&& autoheader \
&& automake --foreign --add-missing --copy \
&& autoconf

